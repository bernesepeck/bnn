{
  "scripts": {
    "copy-assets": "cp -r src/assets/* dist",
    "start": "npm run build-img && parcel src/index.html src/city/index.html",
    "build": "npm run build-img && parcel build src/index.html src/city/index.html",
    "build-img": "parcel build src/assets/* --no-cache"
  },
  "dependencies": {
    "@directus/sdk": "^14.0.0",
    "lit": "^3.1.1"
  },
  "devDependencies": {
    "parcel": "^2.11.0",
    "typescript": "^5.3.3"
  }
}