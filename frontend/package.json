{
  "scripts": {
    "copy-assets": "cp -r src/assets/* dist",
    "start": "npm run copy-assets && npm run build-img && parcel src/index.html src/city/index.html",
    "build": "npm run build-img && parcel build src/index.html src/city/index.html",
    "build-img": "parcel build src/assets/* --no-cache"
  },
  "dependencies": {
    "@directus/sdk": "^19.1.0",
    "@sentry/browser": "^9.25.0",
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "@parcel/packager-xml": "^2.15.2",
    "@parcel/transformer-xml": "^2.15.2",
    "parcel": "^2.15.2",
    "process": "^0.11.10",
    "typescript": "^5.8.3"
  }
}
