{
  "scripts": {
    "start": "npm run build-img && parcel index.html city/index.html",
    "build": "parcel build index.html",
    "build-img": "parcel build assets/* --no-cache"
  },
  "devDependencies": {
    "lit": "^3.1.1",
    "parcel": "^2.11.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@directus/sdk": "^14.0.0"
  }
}
