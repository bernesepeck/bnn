{
  "scripts": {
    "copy-assets": "cp -r src/assets/* dist",
    "start": "npm run copy-assets && npm run build-img && parcel src/index.html src/city/index.html",
    "build": "npm run copy-assets && build src/index.html",
    "build-img": "parcel build src/assets/* --no-cache"
  },
  "devDependencies": {
    "lit": "^3.1.1",
    "parcel": "^2.11.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@directus/sdk": "^14.0.0"
  }
}
